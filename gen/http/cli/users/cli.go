// Code generated by goa v3.2.3, DO NOT EDIT.
//
// users HTTP client CLI support package
//
// Command:
// $ goa gen users/design

package cli

import (
	"encoding/json"
	"flag"
	"fmt"
	"net/http"
	"os"
	rolesc "users/gen/http/roles/client"
	usersc "users/gen/http/users/client"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `users (list|show|add|update|remove|activate)
roles (list|show|add|update|remove)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` users list --view "tiny"` + "\n" +
		os.Args[0] + ` roles list --view "default"` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		usersFlags = flag.NewFlagSet("users", flag.ContinueOnError)

		usersListFlags    = flag.NewFlagSet("list", flag.ExitOnError)
		usersListViewFlag = usersListFlags.String("view", "", "")

		usersShowFlags     = flag.NewFlagSet("show", flag.ExitOnError)
		usersShowEmailFlag = usersShowFlags.String("email", "REQUIRED", "Email of user to show")
		usersShowViewFlag  = usersShowFlags.String("view", "", "")

		usersAddFlags    = flag.NewFlagSet("add", flag.ExitOnError)
		usersAddBodyFlag = usersAddFlags.String("body", "REQUIRED", "")

		usersUpdateFlags     = flag.NewFlagSet("update", flag.ExitOnError)
		usersUpdateBodyFlag  = usersUpdateFlags.String("body", "REQUIRED", "")
		usersUpdateEmailFlag = usersUpdateFlags.String("email", "REQUIRED", "Email of the user")

		usersRemoveFlags     = flag.NewFlagSet("remove", flag.ExitOnError)
		usersRemoveEmailFlag = usersRemoveFlags.String("email", "REQUIRED", "Email of user to remove")

		usersActivateFlags = flag.NewFlagSet("activate", flag.ExitOnError)
		usersActivatePFlag = usersActivateFlags.String("p", "REQUIRED", "[]string is the payload type of the users service activate method.")

		rolesFlags = flag.NewFlagSet("roles", flag.ContinueOnError)

		rolesListFlags    = flag.NewFlagSet("list", flag.ExitOnError)
		rolesListViewFlag = rolesListFlags.String("view", "", "")

		rolesShowFlags    = flag.NewFlagSet("show", flag.ExitOnError)
		rolesShowNameFlag = rolesShowFlags.String("name", "REQUIRED", "Name of role to show")
		rolesShowViewFlag = rolesShowFlags.String("view", "", "")

		rolesAddFlags    = flag.NewFlagSet("add", flag.ExitOnError)
		rolesAddBodyFlag = rolesAddFlags.String("body", "REQUIRED", "")

		rolesUpdateFlags    = flag.NewFlagSet("update", flag.ExitOnError)
		rolesUpdateBodyFlag = rolesUpdateFlags.String("body", "REQUIRED", "")
		rolesUpdateNameFlag = rolesUpdateFlags.String("name", "REQUIRED", "Name of role")

		rolesRemoveFlags    = flag.NewFlagSet("remove", flag.ExitOnError)
		rolesRemoveNameFlag = rolesRemoveFlags.String("name", "REQUIRED", "Name of role to remove")
	)
	usersFlags.Usage = usersUsage
	usersListFlags.Usage = usersListUsage
	usersShowFlags.Usage = usersShowUsage
	usersAddFlags.Usage = usersAddUsage
	usersUpdateFlags.Usage = usersUpdateUsage
	usersRemoveFlags.Usage = usersRemoveUsage
	usersActivateFlags.Usage = usersActivateUsage

	rolesFlags.Usage = rolesUsage
	rolesListFlags.Usage = rolesListUsage
	rolesShowFlags.Usage = rolesShowUsage
	rolesAddFlags.Usage = rolesAddUsage
	rolesUpdateFlags.Usage = rolesUpdateUsage
	rolesRemoveFlags.Usage = rolesRemoveUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "users":
			svcf = usersFlags
		case "roles":
			svcf = rolesFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "users":
			switch epn {
			case "list":
				epf = usersListFlags

			case "show":
				epf = usersShowFlags

			case "add":
				epf = usersAddFlags

			case "update":
				epf = usersUpdateFlags

			case "remove":
				epf = usersRemoveFlags

			case "activate":
				epf = usersActivateFlags

			}

		case "roles":
			switch epn {
			case "list":
				epf = rolesListFlags

			case "show":
				epf = rolesShowFlags

			case "add":
				epf = rolesAddFlags

			case "update":
				epf = rolesUpdateFlags

			case "remove":
				epf = rolesRemoveFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "users":
			c := usersc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = usersc.BuildListPayload(*usersListViewFlag)
			case "show":
				endpoint = c.Show()
				data, err = usersc.BuildShowPayload(*usersShowEmailFlag, *usersShowViewFlag)
			case "add":
				endpoint = c.Add()
				data, err = usersc.BuildAddPayload(*usersAddBodyFlag)
			case "update":
				endpoint = c.Update()
				data, err = usersc.BuildUpdatePayload(*usersUpdateBodyFlag, *usersUpdateEmailFlag)
			case "remove":
				endpoint = c.Remove()
				data, err = usersc.BuildRemovePayload(*usersRemoveEmailFlag)
			case "activate":
				endpoint = c.Activate()
				var err error
				var val []string
				err = json.Unmarshal([]byte(*usersActivatePFlag), &val)
				data = val
				if err != nil {
					return nil, nil, fmt.Errorf("invalid JSON for usersActivatePFlag, \nerror: %s, \nexample of valid JSON:\n%s", err, "'[\n      \"Nostrum voluptas.\",\n      \"Minus corporis repudiandae laudantium eum velit.\",\n      \"Doloribus et nobis sunt qui consequatur sunt.\",\n      \"Sunt quisquam adipisci blanditiis recusandae est.\"\n   ]'")
				}
			}
		case "roles":
			c := rolesc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = rolesc.BuildListPayload(*rolesListViewFlag)
			case "show":
				endpoint = c.Show()
				data, err = rolesc.BuildShowPayload(*rolesShowNameFlag, *rolesShowViewFlag)
			case "add":
				endpoint = c.Add()
				data, err = rolesc.BuildAddPayload(*rolesAddBodyFlag)
			case "update":
				endpoint = c.Update()
				data, err = rolesc.BuildUpdatePayload(*rolesUpdateBodyFlag, *rolesUpdateNameFlag)
			case "remove":
				endpoint = c.Remove()
				data, err = rolesc.BuildRemovePayload(*rolesRemoveNameFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// usersUsage displays the usage of the users command and its subcommands.
func usersUsage() {
	fmt.Fprintf(os.Stderr, `The users service performs user data.
Usage:
    %s [globalflags] users COMMAND [flags]

COMMAND:
    list: List all stored users
    show: Show user by Email
    add: Add new user and return email.
    update: Update existing user and return email.
    remove: Remove user from users data
    activate: Activate users by emails

Additional help:
    %s users COMMAND --help
`, os.Args[0], os.Args[0])
}
func usersListUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] users list -view STRING

List all stored users
    -view STRING: 

Example:
    `+os.Args[0]+` users list --view "tiny"
`, os.Args[0])
}

func usersShowUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] users show -email STRING -view STRING

Show user by Email
    -email STRING: Email of user to show
    -view STRING: 

Example:
    `+os.Args[0]+` users show --email "Quia quis a." --view "default"
`, os.Args[0])
}

func usersAddUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] users add -body JSON

Add new user and return email.
    -body JSON: 

Example:
    `+os.Args[0]+` users add --body '{
      "email": "ehabterra@hotmail.com",
      "firstname": "Ehab",
      "isactive": true,
      "lastname": "Terra",
      "role": "admin"
   }'
`, os.Args[0])
}

func usersUpdateUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] users update -body JSON -email STRING

Update existing user and return email.
    -body JSON: 
    -email STRING: Email of the user

Example:
    `+os.Args[0]+` users update --body '{
      "firstname": "Ehab",
      "isactive": false,
      "lastname": "Terra",
      "role": "admin"
   }' --email "ehabterra@hotmail.com"
`, os.Args[0])
}

func usersRemoveUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] users remove -email STRING

Remove user from users data
    -email STRING: Email of user to remove

Example:
    `+os.Args[0]+` users remove --email "Quis at quod aspernatur quam repellat dolor."
`, os.Args[0])
}

func usersActivateUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] users activate -p JSON

Activate users by emails
    -p JSON: []string is the payload type of the users service activate method.

Example:
    `+os.Args[0]+` users activate --p '[
      "Nostrum voluptas.",
      "Minus corporis repudiandae laudantium eum velit.",
      "Doloribus et nobis sunt qui consequatur sunt.",
      "Sunt quisquam adipisci blanditiis recusandae est."
   ]'
`, os.Args[0])
}

// rolesUsage displays the usage of the roles command and its subcommands.
func rolesUsage() {
	fmt.Fprintf(os.Stderr, `The roles service performs role data.
Usage:
    %s [globalflags] roles COMMAND [flags]

COMMAND:
    list: List all stored roles
    show: Show role by name
    add: Add new role and return name.
    update: Update existing role and return name.
    remove: Remove role from roles data

Additional help:
    %s roles COMMAND --help
`, os.Args[0], os.Args[0])
}
func rolesListUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] roles list -view STRING

List all stored roles
    -view STRING: 

Example:
    `+os.Args[0]+` roles list --view "default"
`, os.Args[0])
}

func rolesShowUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] roles show -name STRING -view STRING

Show role by name
    -name STRING: Name of role to show
    -view STRING: 

Example:
    `+os.Args[0]+` roles show --name "Voluptatem expedita." --view "tiny"
`, os.Args[0])
}

func rolesAddUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] roles add -body JSON

Add new role and return name.
    -body JSON: 

Example:
    `+os.Args[0]+` roles add --body '{
      "description": "Administrator",
      "name": "admin"
   }'
`, os.Args[0])
}

func rolesUpdateUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] roles update -body JSON -name STRING

Update existing role and return name.
    -body JSON: 
    -name STRING: Name of role

Example:
    `+os.Args[0]+` roles update --body '{
      "description": "Administrator"
   }' --name "admin"
`, os.Args[0])
}

func rolesRemoveUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] roles remove -name STRING

Remove role from roles data
    -name STRING: Name of role to remove

Example:
    `+os.Args[0]+` roles remove --name "Pariatur perferendis."
`, os.Args[0])
}
